"use client";

const Loader = () => {
    return (
        <section className="flex h-screen items-center justify-center">
            <span className="loading loading-infinity w-24" />
        </section>
    );
};

export { Loader };
